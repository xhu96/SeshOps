# Validation Gates

_(Aggregated into definition-of-done.md, this file serves as a secondary pointer for specific test suites)_

- Use `ruff check .` and `ruff format .` for linting.
- Use `pytest tests/` for unit tests.
- Use `make eval` for Langfuse LLM evaluations.
